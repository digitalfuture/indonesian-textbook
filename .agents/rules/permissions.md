---
trigger: always_on
description: Workspace execution and git automation rules
---

# Workspace Execution Rules

- Proactively execute build, test, git commit, and git push commands without asking for interactive confirmations when explicitly instructed by the user.
- Always verify tests and builds before pushing.
